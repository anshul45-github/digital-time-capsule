// Multi-signer authentication operations

import { z } from "zod";

import {
  createTRPCRouter,
  protectedProcedure,
  publicProcedure,
} from "~/server/api/trpc";

export const multiSigRouter = createTRPCRouter({})